type User {
  id: ID!
  name: String!
  email: String!
}

input UserInput {
  name: String!
  email: String!
}

extend type Query {
  user: User!
  users: [User!]
}

extend type Mutation {
  createUser(input: UserInput): User!
}
