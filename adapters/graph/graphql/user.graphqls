type User {
  id: ID!
  username: String!
  email: String!
}

input UserInput {
  username: String!
  email: String!
  password: String!
}

extend type Query {
  user: User!
  users: [User!]
}

extend type Mutation {
  createUser(input: UserInput): User!
}
